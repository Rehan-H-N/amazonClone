<!DOCTYPE html>
<html>
<head>
  <title>Your Cart</title>
  <link rel="stylesheet" href="/css/styleHome.css">
</head>
<body>
  <h2>Your Shopping Cart</h2>

<% if (cart.length === 0) { %>
  <p>Your cart is empty.</p>
<% } else { %>
  <!-- <ul class="cart-list">
    <% cart.forEach(item => { %>
      <li class="cart-item">
        <img src="<%= item.image %>" alt="<%= item.name %>" />
        <div>
          <h4><%= item.name %></h4>
          <p>₹<%= item.price %></p>
          <p>Quantity: <%= item.quantity %></p>
        </div>
      </li>
    </ul>
    <% }) %> -->

    <% cart.forEach(item => { %>
      <li class="cart-item">
        <img src="<%= item.image %>" alt="<%= item.name %>" />
        <div>
          <h4><%= item.name %></h4>
          <p>₹<%= item.price %></p>
          <p>Quantity: <%= item.quantity %></p>
    
          <form action="/api/cart/remove/<%= item._id %>" method="POST">
            <button type="submit">Remove</button>
          </form>
        </div>
      </li>
    <% }) %>
    
  
<% } %>

</body>
</html>
